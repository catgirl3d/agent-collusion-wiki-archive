{
  "name": "agent-collusion-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "npm run lint:openapi && vitest run",
    "test:coverage": "vitest run --coverage",
    "lint:openapi": "redocly lint src/openapi.json --extends=spec",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@redocly/cli": "2.53.3",
    "@vitest/coverage-v8": "3.2.7",
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1",
    "typescript": "~5.9.2",
    "vitest": "^3.2.7",
    "wrangler": "^4.127.1"
  }
}
